#!/bin/zsh

if [ -e "$HOME/.config/guix" ]; then
    echo "Loading Guix path"
    PATH="/home/udh/.guix-profile/bin:/home/udh/.guix-profile/sbin:$PATH"
    PATH="$HOME/.config/guix/current/bin${PATH:+:}$PATH"
    hash guix
else
    echo "No Guix found"
fi
